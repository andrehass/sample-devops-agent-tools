# Default Thresholds

> **Status:** placeholder. Default values (overridable via the `thresholds`
> input) will be filled in alongside the checks that use them, one domain at a
> time. See the project spec for the full candidate list (target file size,
> snapshot retention, orphan retention, delete-file threshold, etc.).

| Key | Default | Used by |
|---|---|---|
| _to be added_ | | |
