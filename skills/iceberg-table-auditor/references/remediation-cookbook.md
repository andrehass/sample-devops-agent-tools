# Remediation Cookbook

> **Status:** placeholder. Engine-specific SQL/CLI remediation (Athena
> `OPTIMIZE`/`VACUUM` vs Spark `CALL <catalog>.system.<procedure>(...)`) will
> be added per check as each check in `check-catalog.md` is built and
> reviewed.
